# OS / editor noise
.DS_Store
*.swp
*~
.vscode/
.idea/

# Force field (user must download separately; see README §Setup)
charmm36-jul2022.ff/
01_prep/charmm36-jul2022.ff

# SLURM logs from runs
logs/*.out
logs/*.err

# GROMACS run outputs (in case the template gets used in-place rather than
# cloned to a scratch dir for each system; these should never live in the repo)
*.gro
*.xtc
*.trr
*.cpt
*.edr
*.tpr
*.log
*.ndx
mdout.mdp
\#*\#

# Topology files generated by 01_prep.sh
topol.top
posre*.itp
*_chain_*.itp
